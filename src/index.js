import React from "react";
import ReactDOM from "react-dom/client";
// import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  // componentes que queremos renderizar
  // la funcion App empieza con mayuscula lo culanos indica que es un componente
  // <App /> cierre en la misma etiqueta
  <App />
);
